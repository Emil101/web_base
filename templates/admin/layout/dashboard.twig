{% extends "admin/layout/base.twig" %}

{% block BodyContent %}
    <div class="wrapper theme-1-active pimary-color-blue">
        <!-- Top Menu Items -->
		<nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="row col-12 m-0 p-0">
                <div class="col m-0 p-0">
                    <div class="mobile-only-brand pull-left">
                        <div class="nav-header pull-left">
                            <div class="logo-wrap">
                                <a href="index.html">
                                    <img class="brand-img img" src="/images/logo.png" alt="brand"/>
                                </a>
                            </div>
                        </div>	
                        <a id="toggle_nav_btn" class="toggle-left-nav-btn pull-left" href="javascript:void(0);"><i class="zmdi zmdi-menu"></i></a>
                    </div>
                </div>
                <div class="col m-0 p-0">
                    <div id="mobile_only_nav" class="mobile-only-nav pull-right">
                        <ul class="nav navbar-right top-nav pull-right">
                            <li class="dropdown auth-drp">
                                <a href="#" class="dropdown-toggle pr-0" data-toggle="dropdown">
                                    <img src="/images/user1.png" alt="user_auth" class="user-auth-img img rounded-circle"/>
                                    <span class="user-online-status"></span>
                                </a>
                                <ul class="dropdown-menu user-auth-dropdown" data-dropdown-in="flipInX" data-dropdown-out="flipOutX">
                                    <h5 class="text-center" ><strong>{{ Session['FullName'] }}</strong></h5>
                                    <hr>
                                    <li>
                                        <a href="profile.html"><i class="zmdi zmdi-shield-security"></i><span>Change Password</span></a>
                                    </li>
                                    <li>
                                        <a href="{{path_for('settings')}}"><i class="zmdi zmdi-settings"></i><span>Settings</span></a>
                                    </li>
                                    <li class="divider"></li>
                                <hr>
                                    <li>
                                        <a href="{{ path_for('Logout') }}"><i class="zmdi zmdi-power"></i><span>Log Out</span></a>
                                    </li>
                                </ul>
                            </li>
                        </ul>
                    </div>	
                </div>
            </div>
        </nav>
		<!-- /Top Menu Items -->
		
        <!-- Left Sidebar Menu -->
            {% include 'admin/layout/sidebar.twig' %}
		<!-- /Left Sidebar Menu -->
		
		<div class="page-wrapper">
            <div class="container-fluid">
                <main>
                    {% block mainBlock %}
                        {# Main Block Content #}
                    {% endblock mainBlock %}
                </main>
            </div>
            <!-- Footer -->
            <footer class="footer">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-sm-12 text-right pr-4">
                            <p>2017 &copy; Playbit. Powered by Playbit</p>
                        </div>
                    </div>
                </div>
            </footer>
            <!-- /Footer -->
		</div>

    </div>
    {{ include('admin/utils/modal.addWidget.twig') }}
{% endblock BodyContent %}

{% block script %}{% endblock script %}
        